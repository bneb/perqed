Please solve this specific lemma from Erdős #265.
It verifies the sup-norm of our 2D vector φvr.

```lean
import Mathlib

open Finset Real

noncomputable section

def φ1r (n : ℕ) : ℝ := 1 / (n : ℝ)
def φ2r (n : ℕ) : ℝ := 1 / ((n : ℝ) - 1)
def φvr (n : ℕ) : ℝ × ℝ := (φ1r n, φ2r n)

lemma φvr_norm_eq (L : ℕ) (hL : 2 ≤ L) : ‖φvr L‖ = 1 / ((L : ℝ) - 1) := by sorry
```

Please output the complete, verified Lean 4 code block replacing the sorry. You must use the local Lean 4 REPL to ensure your tactic sequence compiles.
